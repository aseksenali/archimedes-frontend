import React from 'react'
import { Table } from '@devexpress/dx-react-grid'

const StubCellComponent = (props: Table.CellProps) => {
    return (
        <td style={ { borderBottom: '1px solid #EDF5F8' } }>

        </td>
    )
}

export default StubCellComponent