import styled from 'styled-components'

const WeekPickerWrapper = styled.div`
  color: var(--primary-color);
  display: inline-block;
  position: relative;
  width: 22%;
  top: .5em;
  height: content-box;
`

export { WeekPickerWrapper }