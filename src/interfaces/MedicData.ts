export type MedicData = {
    medicId: string,
    medicName: string,
    minimalAppointmentPeriod: number,
    specialties: Array<string>,
    branch: string,
    startDate: string,
    endDate: string
}